apiVersion: v1
kind: ConfigMap
metadata:
  name: effectops-config
data:
  RABBITMQ_HOST: rabbitmq
  RABBITMQ_PORT: "5672"
  MESSAGING_ENABLED: "true"
  CORS_ALLOWED_ORIGINS: https://effectops.example.com

